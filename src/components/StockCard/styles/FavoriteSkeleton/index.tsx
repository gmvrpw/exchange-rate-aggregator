import { styled } from "solid-styled-components";
import Skeleton from "../../../Skeleton";

const FavoriteSkeleton = styled(Skeleton)`
  height: 100%;
  aspect-ratio: 1 / 1;
`;

export default FavoriteSkeleton;
